import 'package:flutter_test/flutter_test.dart';
import 'package:locshare/app.dart';

void main() {
  test('LocShareApp can be constructed', () {
    expect(const LocShareApp(), isA<LocShareApp>());
  });
}
